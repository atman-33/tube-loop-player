/**
 * Clean Mode Index Page
 *
 * Renders the main player interface by reusing the logic and UI from the main application.
 * This ensures feature parity between standard and clean modes.
 */
export { meta } from "../_app._index/route";
export { default } from "../_app._index/route";
